@model IEnumerable<ProblemSolvingPlatform.Models.Classement>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Score)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Rang)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.User)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Score)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Rang)
            </td>
           
            <td>
                <a asp-action="Edit" asp-route-id="@item.ClassementId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.ClassementId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.ClassementId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
